const videoWidth2 = 362.88;
const videoHeight2 = 204.12;    
    
    
    // Video URL from YouTube or Vimeo
    const videoUrl = "videos/happier.mp4";

    // Create the iframe element
    const videoFrame = document.createElement("iframe");

    // Set iframe attributes
    videoFrame.setAttribute("width", videoWidth2);
    videoFrame.setAttribute("height", videoHeight2);
    videoFrame.setAttribute("src", videoUrl);
    videoFrame.setAttribute("frameborder", "0");
    videoFrame.setAttribute("allowfullscreen", "true");

    // Append the iframe to the video container
    const videoContainer = document.getElementById("video-container");
    videoContainer.appendChild(videoFrame);
    







        // Video URL from YouTube or Vimeo
        const videoUrl2 = "videos/stupidCupid.mp4";
    
        // Set the desired width and height for the video

    
        // Create the iframe element
        const videoFrame2 = document.createElement("iframe");
    
        // Set iframe attributes
        videoFrame2.setAttribute("width", videoWidth2);
        videoFrame2.setAttribute("height", videoHeight2);
        videoFrame2.setAttribute("src", videoUrl2);
        videoFrame2.setAttribute("frameborder", "0");
        videoFrame2.setAttribute("allowfullscreen", "true");
    
        // Append the iframe to the video container
        const videoContainer2 = document.getElementById("video-container2");
        videoContainer2.appendChild(videoFrame2);
        






        // Video URL from YouTube or Vimeo
        const videoUrl3 = "videos/1000x.mp4";
    
        // Set the desired width and height for the video

    
        // Create the iframe element
        const videoFrame3 = document.createElement("iframe");
    
        // Set iframe attributes
        videoFrame3.setAttribute("width", videoWidth2);
        videoFrame3.setAttribute("height", videoHeight2);
        videoFrame3.setAttribute("src", videoUrl3);
        videoFrame3.setAttribute("frameborder", "0");
        videoFrame3.setAttribute("allowfullscreen", "true");
    
        // Append the iframe to the video container
        const videoContainer3 = document.getElementById("video-container3");
        videoContainer3.appendChild(videoFrame3);
        




        // Video URL from YouTube or Vimeo
        const videoUrl4 = "videos/carried.mp4";
    
        // Set the desired width and height for the video

    
        // Create the iframe element
        const videoFrame4 = document.createElement("iframe");
    
        // Set iframe attributes
        videoFrame4.setAttribute("width", videoWidth2);
        videoFrame4.setAttribute("height", videoHeight2);
        videoFrame4.setAttribute("src", videoUrl4);
        videoFrame4.setAttribute("frameborder", "0");
        videoFrame4.setAttribute("allowfullscreen", "true");
    
        // Append the iframe to the video container
        const videoContainer4 = document.getElementById("video-container4");
        videoContainer4.appendChild(videoFrame4);
        




        